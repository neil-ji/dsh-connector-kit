# dsh-npm bundle patch: mounts the host-side npm capability (service + tools).
# GitHub steps reuse ctx.github from the dsh-github bundle (loaded separately).
- insert:
    - id: npm
      name: 'dsh-connector-npm'
